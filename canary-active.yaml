apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-app-deploy
  namespace: nodejs-canary
  labels:
    app: nodejs-app
    stage: deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodejs-app
      stage: deploy
  template:
    metadata:
      labels:
        app: nodejs-app
        stage: deploy
    spec:
      containers:
      - name: nodejs-app
        image: nginx:1.23
        env:
        - name: PRODUCTION_STATE
          value: "test"
        ports:
        - containerPort: 80
          protocol: TCP
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"